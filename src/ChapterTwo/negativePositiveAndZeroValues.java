package ChapterTwo;

import java.util.Scanner;

public class negativePositiveAndZeroValues {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int positiveNumbers;
        int negativeNumbers;
        int zero;
        int number;

        System.out.print("Enter a Number: ");
        number = input.nextInt();
        if (number < 0) {
            negativeNumbers = number;
            System.out.printf("%d Negative number: %n%n", negativeNumbers);
        }
        if (number > 0) {
            positiveNumbers = number;
            System.out.printf("Positive Number: %d%n%n", positiveNumbers);
        }
        if (number == 0) {
            zero = number;
            System.out.printf("Zero: %d%n%n", zero);
        }

        System.out.println("Enter a Number ");
        number = input.nextInt();
        if (number < 0) {
            negativeNumbers = number;
            System.out.printf("Negative number: %d%n%n", negativeNumbers);
        }
        if (number > 0) {
            positiveNumbers = number;
            System.out.printf("Positive Number: %d%n%n", positiveNumbers);
        }
        if (number == 0) {
            zero = number;
            System.out.printf("Zero: %d%n%n", zero);
        }
        System.out.print("Enter a Number ");
        number = input.nextInt();
        if (number < 0) {
            negativeNumbers = number;
            System.out.printf("Negative number: %d%n%n", negativeNumbers);
        }
        if (number > 0) {
            positiveNumbers = number;
            System.out.printf("Positive Number: %d%n%n", positiveNumbers);
        }
        if (number == 0) {
            zero = number;
            System.out.printf("Zero: %d%n%n", zero);
        }
        System.out.print("Enter a Number ");
        number = input.nextInt();
        if (number < 0) {
            negativeNumbers = number;
            System.out.printf("Negative number: %d%n%n", negativeNumbers);
        }
        if (number > 0) {
            positiveNumbers = number;
            System.out.printf("Positive Number: %d%n%n", positiveNumbers);
        }
        if (number == 0) {
            zero = number;
            System.out.printf("Zero: %d%n%n", zero);
        }
        System.out.print("Enter a Number ");
        number = input.nextInt();
        if (number < 0) {
            negativeNumbers = number;
            System.out.printf("Negative number: %d%n%n", negativeNumbers);
        }
        if (number > 0) {
            positiveNumbers = number;
            System.out.printf("Positive Number: %d%n%n", positiveNumbers);
        }
        if (number == 0) {
            zero = number;
            System.out.printf("Zero: %d%n%n", zero);
        }

    }
}